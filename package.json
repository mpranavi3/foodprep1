{
  "name": "foodprep1",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:user && npm run build:admin",
    "build:user": "cd userfrontend && npm install && npm run build",
    "build:admin": "cd adminfrontend && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0"
  }
}
